<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical" android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#5C6BC0"
    android:layout_weight="10"
    android:id="@+id/main">

    <TextView
        style="@style/intro_title"
        android:id="@+id/title"
        android:text="View Local Weather"/>
    <TextView
        style="@style/intro_summary"
        android:id="@+id/description"
        android:text="Weather Delta uses your location to get weather right where you're standing."/>

    <LinearLayout
        android:layout_width="fill_parent"
        android:layout_height="0dp"
        android:orientation="vertical"
        android:layout_marginTop="64dp"
        android:gravity="center"
        android:layout_weight="5">

        <ImageView
            android:layout_width="match_parent"
            android:layout_height="120dp"
            android:layout_gravity="center"
            android:paddingLeft="16dp"
            android:paddingRight="16dp"
            android:id="@+id/image"
            android:src="@drawable/high_res_icon2"/>
    </LinearLayout>
    <TextView
        android:layout_width="fill_parent"
        android:layout_height="64dp" />
</LinearLayout>